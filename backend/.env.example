# Example environment variables for the algo-trading backend

# PostgreSQL connection details (either provide components below, or set DATABASE_URL)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=algo_trading
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Optional: Full database URL (overrides the components above)
# Example Neon DB connection (replace with your own credentials):
# DATABASE_URL=postgresql://neondb_owner:password@ep-super-cake-a1fym8nl-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# Redis connection details
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# JWT / Security
SECRET_KEY=your-secret-key
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Broker credentials (demo/example)
BROKER_API_KEY=
BROKER_API_SECRET=

# Optional Redis URL if you want to use a full URL
# REDIS_URL=redis://:password@localhost:6379/0

# Celery specific overrides (if any)
# CELERY_BROKER_URL=${REDIS_URL}
# CELERY_RESULT_BACKEND=${REDIS_URL}
